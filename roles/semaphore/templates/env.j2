##############
# GLOBAL ENV #
##############
# SEMAPHORE_PORT - do not Global
# SEMAPHORE_INTERFACE
{% if semaphore_server_name == '_' -%}
# SEMAPHORE_WEB_ROOT=http://NOT_SET_IF_NOT_SET_semaphore_server_name
{% else -%}
SEMAPHORE_WEB_ROOT=http://{{ semaphore_server_name }}
{% endif -%}
SEMAPHORE_ACCESS_KEY_ENCRYPTION={{ semaphore_access_key_encryption }}
SEMAPHORE_ADMIN_PASSWORD="{{ semaphore_admin_pass }}"
SEMAPHORE_ADMIN_NAME={{ semaphore_admin_name }}
SEMAPHORE_ADMIN_EMAIL={{ semaphore_admin_email }}
SEMAPHORE_ADMIN={{ semaphore_admin_user }}
SEMAPHORE_PLAYBOOK_PATH=/var/lib/semaphore
SEMAPHORE_TMP_PATH=/tmp/semaphore
